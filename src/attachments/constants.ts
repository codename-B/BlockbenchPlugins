/**
 * Constants for attachment module configuration
 */

/** Delay in milliseconds to wait for Blockbench to settle after import before post-processing */
export const IMPORT_SETTLE_DELAY = 100;

/** Debounce delay in milliseconds for attachment discovery/refresh operations */
export const DISCOVERY_DEBOUNCE_MS = 150;

/** Default duration for quick message notifications in milliseconds */
export const QUICK_MESSAGE_DURATION = 3000;

/** Minimum touch target size for accessibility (in pixels) */
export const MIN_TOUCH_TARGET_SIZE = 32;
